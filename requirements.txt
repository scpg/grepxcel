openpyxl>=3.1
defusedxml>=0.7       # openpyxl uses this automatically when present (XXE protection)
regex>=2024.11.6     # ReDoS guard: supports a hard per-match timeout= (stdlib re does not)
structlog>=24.1      # structured logging processor chain for NDJSON output
